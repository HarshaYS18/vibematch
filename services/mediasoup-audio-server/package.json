{
  "name": "vibematch-mediasoup-audio-server",
  "version": "0.1.0",
  "description": "Standalone mediasoup audio SFU proof-of-concept for VibeMatch 17-seat audio rooms.",
  "main": "src/index.js",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/mediasoupServer.js && node --check src/roomManager.js && node --check src/socketHandlers.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "mediasoup": "^3.15.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
