<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Beta/testing only: allows local Wi-Fi IP backend/media URLs such as
         http://10.x.x.x:8000 while production builds should use HTTPS CDN/API. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
