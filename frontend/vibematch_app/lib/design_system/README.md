# FunKey design system boundary

Chunk 1 creates this boundary without changing the existing UI.

Existing visual tokens/widgets remain where they are until migrated deliberately. New shared visual
primitives may move here only when doing so preserves the approved UI baseline. Architecture work
must not use this directory as an excuse to restyle screens.
