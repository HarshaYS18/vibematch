import 'live_room_page_backend_safe.dart';

class LiveRoomPageModular extends LiveRoomPageBackendSafe {
  const LiveRoomPageModular({
    super.key,
    super.roomName,
    super.roomId,
    super.language,
    super.modeTitle,
    super.onlineCount,
  });
}
