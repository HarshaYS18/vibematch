import 'package:flutter/material.dart';

class PublicVibeCard extends StatelessWidget {
  const PublicVibeCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
