# Vibe Match Media Server

Legacy WebRTC MVP media server for historical/internal testing only.

This is not the active production media path.

Active media service: `backend_media/`.

Do not run this server together with `backend_media` during current production testing because the event contracts and ports are different.
