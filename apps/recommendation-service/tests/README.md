# Recommendation tests

The unit suite locks signal parsing and deterministic time-decayed ranking.
Repository architecture guards prohibit database authority and synchronous
Recommendation writes from domain services.
